{
  "name": "testable-canvas-game-vitest",
  "version": "1.0.0",
  "description": "Decoupling game logic from HTML5 Canvas rendering for deterministic unit testing: pure simulation state, headless Vitest runner, zero DOM dependency, and visual snapshots.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
