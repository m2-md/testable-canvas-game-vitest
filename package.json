{
  "name": "testable-canvas-game-vitest",
  "version": "1.0.0",
  "description": "Temiz oda mimarisi: simülasyonu render'dan, rastgeleliği tohumdan, zamanı duvar saatinden ayıran test edilebilir canvas oyunu (Orb Collector)",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
